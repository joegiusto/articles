import React from 'react';

const Mesh = () => (
  <div className='container'>
    <h1>Mesh</h1>
    <p>Explain Mesh.</p>
    {/* Cool if we could override some of the css of the mneu when we are on this page especisally the hamburger button. */}
  </div>
);

export default Mesh;